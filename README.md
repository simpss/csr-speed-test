test script to see how much time does it take to sign CSR's.

to run execute `run.sh`

to clean up generated files execute `git reset --hard && git clean -fd`

To modify count change `count` within run.sh